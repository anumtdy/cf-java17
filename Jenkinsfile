@Library(['piper-lib', 'piper-lib-os']) _
pipeline {
    agent any
     tools{
        maven 'maven'
        }
    stages {
        stage('Hello') {
            steps {
                echo 'Hello'
               }
            
             }

}
}
